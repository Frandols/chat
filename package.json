{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "14.x",
    "npm": "6.x"
  },
  "scripts": {
    "dev": "concurrently \"nodemon --config nodemon.json server/src/app.ts\" \"cd client && npm start\"",
    "start": "node server/dist/src/app.js",
    "build": "cd server && npm install && npm run build && cd .. && cd client && npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
